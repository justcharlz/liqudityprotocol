// SPDX-License-Identifier: UNLICENSED
pragma solidity ^0.8.7;

import "./Proposal.sol";
contract ProposalFactory {

 

    function makeProposal(string _title, string _description, bytes _content) public {
        // Proposal proposal = Proposal(_title, _description, _content);
    }
}